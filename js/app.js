// 此时,只需要将 angular 传入即可!
(function(angular) {
    'use strict';

    // Your starting point. Enjoy the ride!

    // 创建模块
    var app = angular.module('todoApp', ['todoApp.todoCtrl','todoApp.todoSrv']);
})(angular);
